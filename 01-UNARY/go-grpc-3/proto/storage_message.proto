syntax = "proto3";

package techschool.pcbook;

import "memory_message.proto";
option go_package = "../pb;pb";

message Storage {
    enum Driver {
        UNKNOWN = 0;
        SSD = 1;
        HDD = 2;
    }

    Driver driver = 1;
    Memory memory = 2;
}